
# me — пульт управления системой из терминала

**me** — это минималистичный CLI-фреймворк, который превращает сложные команды в простые методы. Один конфиг, одна команда, автокомплит, интерактивный выбор.

Вместо десятков алиасов в `.bashrc` — один файл `~/.config/me/me.conf` с методами, которые работают везде: в терминале, в скриптах, по SSH, на разных машинах.

---

Для Ведения ченджлогов смотрим файл CHANGELOG.md

---

## Проблема, которую решает me

Алиасы в `.bashrc` работают, пока их 5–10. Когда их становится 30+ — начинается хаос:

- **Нет документации** — через месяц не помнишь, что делает `alias rd`.
- **Аргументы только в конец** — нельзя передать путь в середину команды.
- **Не работают в скриптах** — алиасы не видны в неинтерактивном bash.
- **Конфликты имён** — последний алиас перезаписывает предыдущий.
- **Нет проверок** — алиас не проверяет, установлена ли утилита, не обрабатывает ошибки.

**me решает это:** методы — это bash-функции с мета-тегами, самодокументированные, проверяемые, работающие отовсюду.

---

## Примеры «было / стало»

| Было (монстр) | Стало (me) |
|---------------|------------|
| `busctl --user call org.mpris.MediaPlayer2.mpv /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player Pause` | `me pause` |
| `find ~/.config -type f -name "*.conf" -exec grep --color=always -C2 "server" {} \; \| less -R` | `me fi -c "server" ~/.config` |
| `yt-dlp --default-search "ytsearch10:${*:1}" --get-title --get-id --no-warnings 2>/dev/null \| paste - - \| fzf --reverse \| cut -f2 \| xargs -I {} mpv --no-video "https://youtube.com/watch?v={}"` | `me smy "led zeppelin kashmir"` |
| `aria2c --seed-time=0 --max-connection-per-server=16 --split=16 --continue=true --file-allocation=none --allow-overwrite=true --dir=/mnt/downloads "magnet:?xt=urn:btih:..."` | `me tor add "magnet:..."` |

Вместо километровых строк — простые глаголы. Всё остальное внутри методов.

---

## Быстрая установка

```bash
скачать me и распаковать в  ~/.local/bin/me
cd ~/.local/bin/me
bash install_me.sh
source ~/.profile
me
```

Зависимости : `fzf`, `playerctl`, `ripgrep`, `ffmpeg`.

---

## Первый метод за 30 секунд

Открой `~/.config/me/me.conf` и добавь:

```bash
#@method: hello
#@description: Моя первая команда
#@example: me hello
me_method_hello() {
    echo "Привет, терминал!"
}
```

Сохрани, выполни:

```bash
me hello
```

Готово. Метод работает как родная команда.

---

## Интерактивное создание методов — `me create`

Если не хочется вручную открывать редактор и писать блок — используй встроенный генератор:

```bash
me create
```

Скрипт задаст несколько вопросов:

- **Имя метода** — только латиница, цифры и подчёркивания.
- **Описание** — кратко, что делает метод.
- **Пример** — как вызывать метод (по умолчанию подставляется `me <имя>`).
- **Тело команды** — bash-код, который будет выполняться.

После ввода всех данных `me create`:
- генерирует блок с мета-тегами и функцией;
- проверяет синтаксис через `bash -n`;
- добавляет метод в конец `me.conf`;
- выводит сообщение об успехе.

Пример сессии:

```
$ me create
Имя метода: backup
Описание: Создать бэкап домашней директории
Пример: me backup
Тело команды: tar -czf /backup/home.tar.gz ~/ && notify-send "Бэкап" "Готово"
Метод 'backup' создан в /home/user/.config/me/me.conf.
Запусти: me backup
```

### Работа с внешними скриптами

В качестве тела команды можно указать путь к уже существующему скрипту:

- `$HOME/.local/bin/my_script.sh` — любой скрипт в `PATH`.
- `$HOME/.local/bin/me/me_lib/my_method.sh` — библиотечный скрипт из состава `me`.

Если указан путь к скрипту, `me create` сгенерирует метод, который вызывает этот скрипт с переданными аргументами. Например:

```
Тело команды: $HOME/.local/bin/me/me_lib/backup.sh "$@"
```

Это позволяет мгновенно обернуть любой существующий скрипт в метод `me`, не переписывая его код. Метод становится частью экосистемы — с документацией, автокомплитом и возможностью экспорта.

---

## Ключевые возможности

### Интерактивный режим — `me int`

Не помнишь точное имя метода? Набери `me int` — откроется список всех методов с описаниями и примерами. Выбери нужный — команда попадёт в историю, нажми `↑` и выполни.

### Документация встроена

- `me` — список всех методов с описаниями.
- `me -m <метод>` — подробная справка по конкретному методу.

### Версионирование файлов — `cit`

Храни снепшоты файлов в `.cit/` рядом с проектом, откатывайся, смотри diff, удаляй в корзину и восстанавливай. Не требует Git.

- `cit` (или `me cit`) — интерактивное меню.
- `cit -f` — финальная сборка с полным ченджлогом, создаёт `-F` копию.

### Экспорт и импорт методов — `share/import`

- `me share <метод>` — упаковывает метод со всеми зависимостями в `tar.gz`.
- `me import <метод>` — устанавливает метод на другую машину (из файла, папки или URL).

Это позволяет раскатывать набор методов на парк машин за минуты.

### Портативность

Весь `me` живёт в двух директориях: `~/.local/bin/me/` и `~/.config/me/`. Для переноса на другую Linux-машину достаточно скопировать их и добавить `me` в `PATH`. Никаких правок в `.bashrc` не требуется.

---

## Полный список методов

`me` включает ~30 готовых методов:

| Категория | Примеры |
|-----------|---------|
| **Система** | `vol up/down`, `bright up/down`, `upgrade`, `restart`, `offnow`, `stat` |
| **Медиа** | `play/pause/next/prev/toggle`, `smy`, `svy`, `say` |
| **Утилиты** | `fi`, `ff`, `calc`, `id`, `run`, `color`, `uni_tables`, `itag`, `crossfade`, `gifx`, `wfb` |
| **Коммуникация** | `share`, `import`, `create` |
| **Навигация** | `me`, `me -m`, `me int` |

Подробное описание каждого метода — в статье или в справке `me -m <метод>`.

---

## Почему me, а не алиасы или bash-функции

| Характеристика | Алиас | Bash-функция | me-метод |
|----------------|-------|--------------|----------|
| Документация | Нет | Комментарии | Мета-теги + автоподсказка |
| Аргументы | Только хвост | Полные `$@` | Полные `$@` |
| Ветвления | Нет | Да | Да |
| Справка | `grep` | `declare -f` | `me -m` |
| Поиск команд | Вручную | Вручную | `me` — готовый список |
| Централизация | `.bashrc` | Любой файл | Один `me.conf` |
| Работа в скриптах | Нет | Да | Да |
| Переносимость | Только с `.bashrc` | Вручную | Копия двух папок |

---

## Когда me нужен

- У тебя больше 5–10 алиасов.
- Ты администрируешь несколько машин (или live-системы).
- Ты вешаешь скрипты на хоткеи (i3/sway/bspwm).
- Хочешь чистый `.bashrc` без мусора.
- Нужен интерактивный выбор команд через `fzf`.

## Когда me не нужен

- У тебя 3–5 алиасов и всё устраивает.
- Ты не используешь терминал для рутинных операций.
- Нет желания централизовать управление командами.

---

## Ссылки


- **Полная статья:** [ссылка на статью] — философия, архитектура, все методы, примеры из практики, масштабирование и `cit`.
- **Лицензия:** GNU GPLv3

---

*me v3.5. Совместимость: Bash 4.0+, POSIX sh для внутренних методов.*
```

---
